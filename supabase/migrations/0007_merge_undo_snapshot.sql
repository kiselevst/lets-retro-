-- Let's Retro — поддержка разъединения склеенных карточек.
--
-- Хранит "снимок" состояния прямо перед последней склейкой: текст цели до
-- склейки + кто голосовал за цель и за источник. Разъединение читает этот
-- снимок, чтобы честно восстановить обе карточки и их голоса, а не просто
-- обнулить всё. Отменить можно только последнюю склейку (не всю историю) —
-- при новой склейке снимок перезаписывается.

alter table cards add column last_merge_snapshot jsonb;
