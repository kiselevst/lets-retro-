# Беклог

Задачи, которые осознанно отложены — обсуждали, решили не чинить прямо сейчас.
Берём отсюда, когда явно попросишь.

## Не в работе

1. **Страница входа — добавить возможность править кол-во голосов.**
   Со слов заказчика (2026-08-29). Формулировка пока в исходном виде — при
   взятии в работу уточнить, какая именно страница имеется в виду (лендинг
   `/`, форма создания `/create`, где кол-во голосов уже редактируется, или
   форма входа участника в комнату) и что значит "править" в этом контексте.
2. Подумать ещё.
3. **Возможность разъединить склеенные карточки.**
   Со слов заказчика (этап 6). Сейчас склейка (drag&drop) — необратимая
   операция: карточка-источник удаляется целиком, разъединить обратно
   нечем. При взятии в работу — продумать, как хранить достаточно данных
   для отката (сейчас в `sources` остаётся только текст+автор, не сам объект
   карточки) и как это должно выглядеть в интерфейсе.
4. **Что делать с голосами при склейке карточек.**
   Сейчас голоса карточки-источника теряются (удаляются каскадно вместе с
   ней) — см. комментарий в `lib/cards.ts` (`mergeCards`). Решить: переносить
   голоса на целевую карточку (с дедупликацией по участнику) или оставить
   как есть и просто явно предупреждать в интерфейсе перед склейкой.
5. **Карточка "SMART" выглядит не секси.**
   Со слов заказчика (этап 6): нынешнее отображение (подписанные строки
   "Задача / Успех / Кто / Срок") функционально, но не проработано визуально.
   При взятии в работу — отдельный проход по дизайну этой карточки.
6. **Переключатель тёмная/светлая тема.**
   Со слов заказчика (этап 6). Сейчас только тёмная тема (палитра зашита в
   `tailwind.config.ts`). При взятии в работу — понадобится светлый вариант
   палитры и способ хранить выбор пользователя (скорее всего localStorage,
   без завязки на аккаунт, раз аутентификации нет).
7. **Перенести кнопку "Ретро закончилось" в правый нижний угол экрана.**
   Со слов заказчика (этап 8). Сейчас она в общем ряду шапки доски рядом с
   "Показать/скрыть карточки" — та остаётся на месте, переносится только
   "Ретро закончилось" (как плавающая кнопка в углу, отдельно от панели
   сверху). Черновой вариант делался (`fixed bottom-6 right-6`), но откатили
   по просьбе — не решили сразу.

## Сделано (для истории, не переносить обратно в "не в работе")

- ~~Ctrl/Cmd+Enter для сохранения карточки~~ — добавлено при том же обсуждении,
  куда просили записать в беклог, но фикс оказался тривиальным и сделан сразу.
